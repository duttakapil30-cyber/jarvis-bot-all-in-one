module.exports = {
    name: 'ping',
    description: 'Check JARVIS latency',
    execute(message, args) {
        message.channel.send(`Pinging... 🛰️`).then(sent => {
            sent.edit(`Pong! Latency: ${sent.createdTimestamp - message.createdTimestamp}ms`);
        });
    },
};
